# Task Logs

## 2026-06-16

- Реализован блок 5: добавлен CLI управления SQLite-БД с миграциями, rollback через down-файлы, seed/reset/check, Makefile-обвязка и тесты команд.
- Добавлен бизнес-запрос «SIM-карты без активных услуг»: `queries/sim-cards-without-active-services.sql` (NOT EXISTS по `sim_card_services.status = 'active'`, без фильтра по статусу карты) и тест `test_queries.py` на seed (ожидаются id 3, 4, 5, 6, 8, 9).

## 2026-06-08

- Реализован блок 4: `seed.sql` заменён на полный идемпотентный набор вымышленных данных (клиенты, SIM-карты в разных статусах, 8 услуг, подключения, платежи и статьи базы знаний). Добавлен `test_seed.py`, который проверяет повторную загрузку без дублей, ожидаемые количества записей и наличие ключевых статусов SIM-карт/платежей.

## 2026-06-05

- По Word-требованию CR-2026-014 создана миграция `003_add_esim_support.sql`: поддержка eSIM в `sim_cards` — поля `sim_type` ('physical'/'esim', default 'physical'), `eid` (32 цифры, UNIQUE среди eSIM через partial-индекс, NULL у физических) и статус `qr_generated`. Расширить CHECK статуса в SQLite через ALTER нельзя, поэтому таблица безопасно пересобирается (create→copy→drop→rename) с `legacy_alter_table=ON` вокруг RENAME (иначе ломается о триггер payments, ссылающийся на sim_cards). Данные не теряются, UNIQUE на iccid/phone сохранены, триггер `trg_sim_cards_client_update` пересоздан. `schema.sql` синхронизирован, `test_migrations.py` обновлён — 3 passed.
- В миграцию `001_create_tables.sql` добавлены русские комментарии: описание каждой таблицы и назначения полей, а также пояснения к триггерам. SQL без изменений.
- Реализован блок 3 (миграции): 001 — таблицы и триггеры из схемы, 002 — индексы, 003 — справочник услуг, сгенерированный из Excel-прайса (12 валидных строк из 16; маппинг рус.→enum, 4 «грязные» строки исключены), 004 — поддержка eSIM (колонки `sim_type`/`eid`, статус `qr_generated`) через безопасное перестроение `sim_cards`. Ключевое решение: расширить CHECK статуса в SQLite нельзя через ALTER, поэтому таблица пересоздаётся по официальной процедуре с `foreign_keys=OFF` и `legacy_alter_table=ON` (иначе RENAME ломается о триггеры payments, ссылающиеся на sim_cards).
- Добавлен минимальный применятель миграций (`migrate.py`, только для тестов): учёт в `migration_history`, атомарность каждой миграции (BEGIN/COMMIT с откатом), идемпотентность по версии. Тесты `test_migrations.py` проверяют сборку пустой БД, число услуг = числу валидных строк Excel (динамически), идемпотентность и откат при ошибке. pytest зелёный (10 passed). Полноценный CLI — задача блока 5.
- Добавлены демо-файлы в `project/files/` для этапа 9 (работа с форматами): Excel-прайс услуг, Word-ТЗ на eSIM, PDF-спека платёжного API, CSV юрлиц, Word-статьи базы знаний, PNG-скриншот и log ошибки. Содержимое согласовано со схемой, в данные внесены намеренные дефекты (дубли ИНН, невалидная длина ИНН, пустой тип/цена услуги) для демонстрации разбора ИИ.
- Цель `make db-migrate` подключена к реальному применятелю: добавлена тонкая точка входа в `migrate.py` (аргумент — путь к БД), цель зависит от `db-create`. Полноценный CLI (rollback/seed/reset) остаётся за Блоком 5. Проверено: применение на `data/telecom.db` (12 услуг, eSIM-поля), повторный прогон идемпотентен.
- Реализована цель `make db-seed`: применяет `seeds/*.sql` к БД (зависит от `db-migrate`, FK включены). Сами seed-данные генерируются отдельно (Блок 4). Проверено вставкой и возвратом БД в чистое до-сидовое состояние.
- Сгенерирован `seeds/seed.sql` из Excel-прайса (`files/tariffs/услуги-тарифы-2026.xlsx`): 12 валидных строк услуг из 16, маппинг рус.→enum (тип/период/статус), регистр нормализован. Исключены 4 «грязные» строки: пустой тип, пустая и отрицательная цена, статус «черновик» вне enum. Проверено сборкой in-memory SQLite поверх 001 (12 строк).
- Готовые «данные» убраны из миграций ради live-демо: справочник услуг генерится в сиды из Excel, а пример изменения схемы (eSIM) создаётся вживую из Word-требования. В репозитории остаются только базовые миграции 001/002. Удалены `003_seed_services.sql`, `003_example_alter.sql`, `scripts/services_from_excel.py`; `test_migrations.py` сведён к набору 001/002. Добавлены цели `make db-clean` (очистка данных) и `make db-rollback` (удаление всех таблиц). Из Word-доки убран номер миграции. Граница: миграции — структура, данные — сиды. Тесты — 10 passed.

## 2026-06-03

- Реализован блок 2: добавлена SQLite-схема клиентов, SIM-карт, услуг, платежей и базы знаний с ключами, ограничениями и индексами; настроены pytest-проверки схемы через временную БД.
- После проверки `tester` расширено покрытие тестов схемы по UNIQUE/FK/CHECK и добавлен комментарий о рисках хранения паспортных данных в учебной БД.
- По итогам архитектурного ревью усилены инварианты: detail-таблицы соответствуют типу клиента, платеж по SIM-карте привязан к ее владельцу, история подключений услуг поддерживает повторные подключения.

