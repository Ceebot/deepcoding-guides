# Task Logs

## 2026-06-05

- Реализован блок 3 (миграции): 001 — таблицы и триггеры из схемы, 002 — индексы, 003 — справочник услуг, сгенерированный из Excel-прайса (12 валидных строк из 16; маппинг рус.→enum, 4 «грязные» строки исключены), 004 — поддержка eSIM (колонки `sim_type`/`eid`, статус `qr_generated`) через безопасное перестроение `sim_cards`. Ключевое решение: расширить CHECK статуса в SQLite нельзя через ALTER, поэтому таблица пересоздаётся по официальной процедуре с `foreign_keys=OFF` и `legacy_alter_table=ON` (иначе RENAME ломается о триггеры payments, ссылающиеся на sim_cards).
- Добавлен минимальный применятель миграций (`migrate.py`, только для тестов): учёт в `migration_history`, атомарность каждой миграции (BEGIN/COMMIT с откатом), идемпотентность по версии. Тесты `test_migrations.py` проверяют сборку пустой БД, число услуг = числу валидных строк Excel (динамически), идемпотентность и откат при ошибке. pytest зелёный (10 passed). Полноценный CLI — задача блока 5.
- Добавлены демо-файлы в `project/files/` для этапа 9 (работа с форматами): Excel-прайс услуг, Word-ТЗ на eSIM, PDF-спека платёжного API, CSV юрлиц, Word-статьи базы знаний, PNG-скриншот и log ошибки. Содержимое согласовано со схемой, в данные внесены намеренные дефекты (дубли ИНН, невалидная длина ИНН, пустой тип/цена услуги) для демонстрации разбора ИИ.
- Цель `make db-migrate` подключена к реальному применятелю: добавлена тонкая точка входа в `migrate.py` (аргумент — путь к БД), цель зависит от `db-create`. Полноценный CLI (rollback/seed/reset) остаётся за Блоком 5. Проверено: применение на `data/telecom.db` (12 услуг, eSIM-поля), повторный прогон идемпотентен.
- Реализована цель `make db-seed`: применяет `seeds/*.sql` к БД (зависит от `db-migrate`, FK включены). Сами seed-данные генерируются отдельно (Блок 4). Проверено вставкой и возвратом БД в чистое до-сидовое состояние.
- Услуги вынесены из миграций в сиды: удалены `migrations/003_seed_services.sql` и `scripts/services_from_excel.py`, eSIM-миграция переномерована 004→003 (без пропусков), `test_migrations.py` приведён к набору 001/002/003 (services создаётся пустой). Граница уточнена: миграции — только структура, все данные (включая услуги) — сиды. Добавлена цель `make db-clean` (очистка данных, история миграций сохраняется). БД пересобрана. Тесты — 10 passed.

## 2026-06-03

- Реализован блок 2: добавлена SQLite-схема клиентов, SIM-карт, услуг, платежей и базы знаний с ключами, ограничениями и индексами; настроены pytest-проверки схемы через временную БД.
- После проверки `tester` расширено покрытие тестов схемы по UNIQUE/FK/CHECK и добавлен комментарий о рисках хранения паспортных данных в учебной БД.
- По итогам архитектурного ревью усилены инварианты: detail-таблицы соответствуют типу клиента, платеж по SIM-карте привязан к ее владельцу, история подключений услуг поддерживает повторные подключения.

