# Секреты и учётные данные — не подмешивать в контекст агента
.env
.env.*
*.pem
*.key
creds.md
**/credentials.json

# Приватные данные проекта — вне авто-контекста
# (терминал и MCP этот файл не ограничивает — см. этап 13)
project/data/*.db
